# Speedtest Monitor

To repo zawiera:

- `app/` — kontenerową aplikację (FastAPI + SQLite) z UI i raportami,
- `speedtest.py` — stary skrypt (legacy).

## Szybki start

Dokumentacja uruchomienia i konfiguracji: `app/README.md`.

